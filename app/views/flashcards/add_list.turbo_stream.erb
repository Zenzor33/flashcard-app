<%= turbo_stream.replace "flash", partial: "layouts/flash" %>

<%= turbo_stream.replace dom_id(@flashcard) do%>
  <%= render partial: "flashcards/line_item", locals: {flashcard: @flashcard} %>
<%end%>