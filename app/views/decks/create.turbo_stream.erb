<!-- This refreshes the flashcard turboframe, at present only serves to update the state of the add and remove buttons for each flashcard. -->
<%= turbo_stream.replace(dom_id(@flashcard)) do%>
  <%= render partial: "flashcards/flashcard", locals: {flashcard: @flashcard} %>
<%end%>